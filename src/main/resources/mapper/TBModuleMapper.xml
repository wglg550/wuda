<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qmth.wuda.teaching.dao.TBModuleMapper">

    <delete id="deleteAll">
        delete t from t_b_module t
        <where>
            <if test="paperCode != null and paperCode != ''">
                and t.paper_code = #{paperCode}
            </if>
            <if test="courseCode != null and courseCode != ''">
                and t.course_code = #{courseCode}
            </if>
            <if test="moduleNames != null">
                and t.name in
                <foreach collection="moduleNames" item="moduleName" index="index" open="(" close=")" separator=",">
                    #{moduleName}
                </foreach>
            </if>
        </where>
    </delete>

    <select id="findByPaperCodeAndCourseCode" resultType="com.qmth.wuda.teaching.entity.TBModule">
        select * from t_b_module tbm
        <where>
            <if test="paperCode != null and paperCode != ''">
                and tbm.paper_code = #{paperCode}
            </if>
            <if test="courseCode != null and courseCode != ''">
                and tbm.course_code = #{courseCode}
            </if>
        </where>
    </select>
</mapper>
